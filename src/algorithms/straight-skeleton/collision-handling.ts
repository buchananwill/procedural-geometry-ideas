import {CollisionEvent, StraightSkeletonSolverContext} from "@/algorithms/straight-skeleton/types";

export function handleCollisionEvent(event: CollisionEvent, context: StraightSkeletonSolverContext){
    /*
    * Possible scenarios:
    * 1. Adja
    * */
}